<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="ManholeCardManager.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ManholeCardManager"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid Background="{ThemeResource
 ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ヘッダー -->
        <Grid Grid.Row="0" Padding="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" x:Name="AppTitleBlock"
                       Style="{StaticResource TitleTextBlockStyle}"/>

            <StackPanel Grid.Column="1" Orientation="Horizontal"
                        Spacing="10">
                <TextBlock x:Name="LanguageLabelBlock"
                           VerticalAlignment="Center"
                           Style="{StaticResource
                           BodyStrongTextBlockStyle}"/>
                <ComboBox x:Name="LanguageComboBox" Width="150"
                          ItemsSource="{Binding
                          LocalizationService.SupportedLanguages}"
                          SelectedValuePath="LanguageCode"
                          SelectionChanged="LanguageComboBox_SelectionChanged">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding LanguageName}"/>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
            </StackPanel>
        </Grid>

        <!-- メインコンテンツ -->
        <Grid Grid.Row="1" Padding="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- 検索 -->
            <StackPanel Grid.Row="0" Spacing="10" Margin="0,0,0,15">
                <TextBlock x:Name="DistributionLocationsLabel"
                           Text="配布場所一覧"
                           Style="{StaticResource
                           SubtitleTextBlockStyle}"/>
                <Grid ColumnSpacing="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="200"/>
                    </Grid.ColumnDefinitions>
                    <TextBox Grid.Column="0" x:Name="LocationSearchBox" Height="40"
                             PlaceholderText="場所名で検索..."
                             Text="{Binding
                             DistributionLocationViewModel.SearchText,
                             Mode=TwoWay,
                             UpdateSourceTrigger=PropertyChanged}"/>
                    <ComboBox Grid.Column="1" x:Name="SeriesNumberFilterComboBox" 
                              Height="40"
                              ItemsSource="{Binding
                              DistributionLocationViewModel.SeriesNumbers}"
                              SelectedItem="{Binding
                              DistributionLocationViewModel.SelectedSeriesNumber,
                              Mode=TwoWay}"
                              SelectionChanged="SeriesNumberFilterComboBox_SelectionChanged"
                              DropDownOpened="SeriesNumberFilterComboBox_DropDownOpened">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding}"/>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                </Grid>
            </StackPanel>

            <!-- カード一覧 -->
            <ScrollViewer Grid.Row="1">
                <ItemsControl ItemsSource="{Binding
                              DistributionLocationViewModel.Locations}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Spacing="15" Margin="0,0,0,20">
                                <!-- 配布場所ヘッダー -->
                                <StackPanel Spacing="5">
                                    <TextBlock Text="{Binding
                                               LocationName}"
                                               FontSize="20"
                                               FontWeight="Bold"/>
                                    <TextBlock Text="{Binding Address}"
                                               FontSize="12"/>
                                </StackPanel>

                                <!-- 配布場所のカード一覧 -->
                                <ItemsControl ItemsSource="{Binding
                                              DistributedCards}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <StackPanel Spacing="10"/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border CornerRadius="12"
                                                    Padding="20"
                                                    Background="{ThemeResource
                                                    CardBackgroundFillColorDefaultBrush}"
                                                    BorderBrush="{ThemeResource
                                                    CardStrokeColorDefaultBrush}"
                                                    BorderThickness="1">
                                                <Grid ColumnSpacing="15">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition
                                                            Width="120"/>
                                                        <ColumnDefinition
                                                            Width="*"/>
                                                    </Grid.ColumnDefinitions>

                                                    <!-- 左側：カードデザイン画像風 -->
                                                    <StackPanel
                                                        Grid.Column="0"
                                                        Spacing="8">
                                                        <Border
                                                            CornerRadius="8"
                                                            Background="#E8F5E9"
                                                            Height="140"
                                                            BorderBrush="#4CAF50"
                                                            BorderThickness="3">
                                                            <Grid>
                                                                <!-- カードデザイン画像 -->
                                                                <Image
                                                                    Source="{Binding CachedImagePath}"
                                                                    Stretch="Uniform"/>
                                                                
                                                                <!-- 取得ステータス表示（画像の上に重ねる） -->
                                                                <Button
                                                                    Background="Transparent"
                                                                    BorderThickness="0"
                                                                    Padding="8"
                                                                    HorizontalAlignment="Left"
                                                                    VerticalAlignment="Top"
                                                                    Click="AcquisitionStatus_Click">
                                                                    <Grid>
                                                                        <Ellipse
                                                                            Width="32"
                                                                            Height="32"
                                                                            Fill="{Binding
                                                                            IsAcquired,
                                                                            Converter={StaticResource
                                                                            AcquisitionStatusColorConverter}}"/>
                                                                        <TextBlock
                                                                            Text="{Binding
                                                                            AcquisitionStatusDisplay}"
                                                                            FontSize="20"
                                                                            FontWeight="Bold"
                                                                            HorizontalAlignment="Center"
                                                                            VerticalAlignment="Center"
                                                                            Foreground="White"/>
                                                                    </Grid>
                                                                </Button>
                                                            </Grid>
                                                        </Border>
                                                        <TextBlock
                                                            Text="{Binding
                                                            LocationDisplay}"
                                                            FontSize="11"
                                                            TextAlignment="Center"
                                                            TextWrapping="Wrap"/>
                                                    </StackPanel>

                                                    <!-- 右側：カード情報 -->
                                                    <StackPanel
                                                        Grid.Column="1"
                                                        Spacing="8">
                                                        <!-- 発行日 -->
                                                        <StackPanel Spacing="2">
                                                            <TextBlock
                                                                Text="発行日"
                                                                FontSize="11"
                                                                FontWeight="Bold"/>
                                                            <TextBlock
                                                                Text="{Binding
                                                                IssuedDateDisplay}"
                                                                FontSize="13"/>
                                                        </StackPanel>

                                                        <!-- 詳細情報グリッド -->
                                                        <Grid
                                                            ColumnSpacing="15"
                                                            RowSpacing="8"
                                                            Margin="0,8,0,0">
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition
                                                                    Width="*"/>
                                                                <ColumnDefinition
                                                                    Width="*"/>
                                                            </Grid.ColumnDefinitions>
                                                            <Grid.RowDefinitions>
                                                                <RowDefinition
                                                                    Height="Auto"/>
                                                                <RowDefinition
                                                                    Height="Auto"/>
                                                                <RowDefinition
                                                                    Height="Auto"/>
                                                            </Grid.RowDefinitions>

                                                            <!-- 弾数 -->
                                                            <StackPanel
                                                                Grid.Row="0"
                                                                Grid.Column="0"
                                                                Spacing="2">
                                                                <TextBlock
                                                                    Text="弾数"
                                                                    FontSize="11"
                                                                    FontWeight="Bold"/>
                                                            </StackPanel>

                                                            <!-- 概要 -->
                                                            <StackPanel
                                                                Grid.Row="0"
                                                                Grid.Column="1"
                                                                Spacing="2">
                                                                <TextBlock
                                                                    Text="概要"
                                                                    FontSize="11"
                                                                    FontWeight="Bold"/>
                                                                <TextBlock
                                                                    Text="{Binding
                                                                    DescriptionDisplay}"
                                                                    FontSize="13"
                                                                    TextWrapping="Wrap"/>
                                                            </StackPanel>

                                                            <!-- 都道府県 -->
                                                            <StackPanel
                                                                Grid.Row="1"
                                                                Grid.Column="0"
                                                                Spacing="2">
                                                                <TextBlock
                                                                    Text="都道府県"
                                                                    FontSize="11"
                                                                    FontWeight="Bold"/>
                                                                <TextBlock
                                                                    Text="{Binding
                                                                    Prefecture}"
                                                                    FontSize="11"
                                                                    TextWrapping="Wrap"/>
                                                            </StackPanel>

                                                            <!-- 在庫状況 -->
                                                            <StackPanel
                                                                Grid.Row="1"
                                                                Grid.Column="1"
                                                                Spacing="2">
                                                                <TextBlock
                                                                    Text="在庫状況"
                                                                    FontSize="11"
                                                                    FontWeight="Bold"/>
                                                                <StackPanel
                                                                    Orientation="Horizontal"
                                                                    Spacing="5">
                                                                    <Button
                                                                        Content="{Binding
                                                                        StockStatus}"
                                                                        Tag="{Binding
                                                                        StockStatus}"
                                                                        Click="StockStatusLink_Click"
                                                                        Style="{StaticResource
                                                                        TextBlockButtonStyle}"
                                                                        FontSize="13"
                                                                        Padding="0"
                                                                        MinWidth="0"
                                                                        MinHeight="0"
                                                                        Foreground="{ThemeResource
                                                                        AccentTextFillColorPrimaryBrush}"
                                                                        Visibility="{Binding
                                                                        StockStatus,
                                                                        Converter={StaticResource
                                                                        IsUrlConverter}}"/>
                                                                    <TextBlock
                                                                        Text="{Binding
                                                                        StockStatus}"
                                                                        FontSize="13"
                                                                        Visibility="{Binding
                                                                        StockStatus,
                                                                        Converter={StaticResource
                                                                        IsUrlConverter},
                                                                        ConverterParameter=false}"/>
                                                                    <TextBlock
                                                                        Text="{Binding
                                                                        StockCountDisplay}"
                                                                        FontSize="13"/>
                                                                </StackPanel>
                                                            </StackPanel>
                                                        </Grid>
                                                    </StackPanel>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>

                                <Border Height="1"
                                        Background="{ThemeResource
                                        DividerStrokeColorDefaultBrush}"/>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>

        <TeachingTip x:Name="LoadingTip"
                     IsOpen="{Binding IsLoading}"
                     IsLightDismissEnabled="False"/>
    </Grid>
</Window>
